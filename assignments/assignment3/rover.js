const Message = require('../assignment3/message.js');

let message = new Message('dave')

class Rover {
    constructor(position, mode = 'NORMAL', generatorWatts = 110) {
        this.position = position
    }
    receiveMessage(message) {
        let obj = {
            message: Message,
            result: 
        }
        return

        }
}

module.exports = Rover;